package databaze

func Push() {
	/* f, _ := os.Open("C:/Users/Firu/Downloads/lekce.csv")
	csvReader := csv.NewReader(f)
	records, _ := csvReader.ReadAll()

	objekty := []*modely.Slovnik{}
	for _, v := range records {
		objekty = append(objekty, &modely.Slovnik{Slovo: v[0]})
	}
	DB.Create(objekty) */
}
